# Database Configuration
# For local development, you can use these defaults
DATABASE_URL=postgres://postgres:mypassword@localhost:5432/friendsdb
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=mypassword
DB_NAME=friendsdb

# Note: Database names must follow pattern: /(^[a-z_][a-z0-9_]*$)|(^$)/
# Valid examples: friendsdb, friends_db, friends
# Invalid examples: friends-db, FriendsDB, friends.db

# For Render deployment, these will be automatically set by Render
# DATABASE_URL will be provided by Render's PostgreSQL service
# You don't need to set individual DB_* variables when using DATABASE_URL
